# * TYPES

interface MutationResponse {
  success: Boolean!
}

type MutationResponseWithId implements MutationResponse {
  id: ID!
  success: Boolean!
}

# * QUERIES

# queries default to requiring admin permissions
type Query @aws_cognito_user_pools(cognito_groups: ["admin"]) {
  _: Boolean # stub
}

# * MUTATIONS

# mutations default to requiring admin permissions, or api_key
type Mutation @aws_cognito_user_pools(cognito_groups: ["admin"]) {
  _: Boolean # stub
}
