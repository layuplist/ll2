# * SCALARS

# appsync
scalar AWSDate
scalar AWSTime
scalar AWSDateTime
scalar AWSTimestamp
scalar AWSEmail
scalar AWSJSON
scalar AWSPhone
scalar AWSURL
scalar AWSIPAddress

# * TYPES

interface MutationResponse {
  success: Boolean!
}

type MutationResponseWithId implements MutationResponse {
  id: ID!
  success: Boolean!
}

# * QUERIES

type Query {
  _: Boolean # stub
}

# * MUTATIONS

type Mutation {
  _: Boolean # stub
}
