# * SCALARS

scalar AWSDate

# * TYPES

interface MutationResponse {
  success: Boolean!
}

type MutationResponseWithId implements MutationResponse {
  id: ID!
  success: Boolean!
}

# * QUERIES

type Query {
  _: Boolean # stub
}

# * MUTATIONS

type Mutation {
  _: Boolean # stub
}
