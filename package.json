{
  "name": "layuplist",
  "version": "1.0.0",
  "repository": "https://github.com/layuplist/ll2",
  "author": "dplanner-dev",
  "license": "MIT",
  "workspaces": ["schema", "backend", "client", "infra"],
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "deploy": "yarn build && yarn workspace @layuplist/infra deploy --all"
  }
}
